# User Guide

Run `uv run weavevision doctor` first. Prepare and verify a dataset before training. Use
`uv run weavevision serve` for the local interface. When no eligible model and locked
threshold exist, the UI remains operational but analysis reports model-not-ready. Review
anomaly overlays as evidence, not as an automatic physical quality grade.
